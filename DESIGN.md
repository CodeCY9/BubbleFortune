# BubbleFortune 视觉与交互规范

<!-- impeccable:design-schema 1 -->

更新：2026-09-05。依据：[GDD](GDD_V2.0.md) §2、§3、§10–14、§17、§19。本文件定义目标表现，不表示现有 UI 已全部符合规范；实际进度见 [ROADMAP](docs/ROADMAP.md)。

## Direction Contract

- **THESIS**：原创资本谈判舞台。深靛紫环境、金属金色箱体边缘、暖聚光灯和局部玻璃面板，强调开箱悬念与报价抉择。
- **OWN-WORLD**：延续紫金方向；个人箱子、当前报价与主操作采用金色，低额蓝色、高额玫红。箱体有真实体积、铰链和材质反差。氛围服务当前事件，避免所有区域同时闪动。
- 金色不表示系统建议接受；金额分组不等于成功/失败。箱子、舞台、角色和音效保持原创。

## 数据与版本边界

- V0.1 只保留 26 箱服务端权威经典模式；不再规划独立 16 箱快速模式。
- 用户名、箱号、报价、轮次、倒计时、剩余开箱数来自授权状态；22 号个人箱、84,000 报价仅是设计示例，禁止写死。
- 金额仍为虚拟分数；2026-09-08 用户确认展示可不带前缀，本轮统一为带千位分隔的纯数字，不在箱内追加“分”。
- 模式在开局配置中选择；局内轮次是状态文本，不是任意修改的下拉菜单。
- 不展示理论期望、内部系数、报价期望百分比、接受建议或箱子概率推断。

## 信息优先级

当前任务 → 当前报价/剩余开箱数 → 合法操作和截止时间 → 个人箱子/公开金额/对手状态 → 历史与装饰。
3D 负责舞台、角色、箱子和效果；关键文字、金额、编号的可访问表达和操作使用 HTML/CSS。音效必须有视觉反馈。

## Design Tokens & Primitives

沿用 src/index.css 现有变量名，后续补齐，不建立重复 Token 系统。

| 用途 | Token/约定 | 值 |
|---|---|---|
| 背景 | --bg-base | #0B0C1E |
| 舞台 | --bg-stage | #1A1B41 到 #0B0C1E 径向渐变 |
| 面板 | --bg-panel | rgba(26,27,65,0.6)，节能档可不透明 |
| 主要文字 | --text-primary | #FFFFFF |
| 次要文字 | --text-secondary | #9CA3AF |
| 报价/个人箱 | --text-gold | #FACC15 |
| 金属/主操作 | --gold-primary | #F59E0B |
| 低额 | --low-tier-blue | #3B82F6，文字可用 #60A5FA |
| 高额 | --high-tier-pink | #F472B6 |

UI 字体优先系统中文无衬线，现有字体可复用，未加载时可靠回退。金额使用 tabular-nums。建议报价 32–64px 随容器调整、正文 14–16px、关键操作不少于 16px；最长金额与大字体模式优先于固定字号。

建议间距 4/8/12/16/24/32px，卡片圆角 12–16px，核心触控区至少 44×44 CSS px。以上是项目设计约定，不冒充 GDD 原始数值。局部玻璃模糊不堆叠全屏滤镜，低画质改实体背景与轻边框。按实际背景检查对比度，金额状态使用文字、划除、图标辅助颜色。

## Responsive Layout Rules

| 宽度 CSS px | 布局 |
|---|---|
| 360–389 | 单列：紧凑状态栏、舞台、底部操作、金额抽屉 |
| 390–599 | 单列：较大舞台、金额双列弹层 |
| 600–899 | 平板竖屏：上下分区、可展开面板 |
| 900–1199 | 简化桌面：收窄面板、顶部对手条 |
| 1200–1599 | 完整三栏：低额、舞台与报价、高额 |
| 1600+ | 舞台居中，限制信息面板最大宽度 |

GDD 的 320–389 断点包含最低支持宽度以下区域；360 以下作为额外兼容，不宣称完整支持。断点是基线，组件优先容器适配。
桌面金额栏可参考 240–280px，但不能挤压舞台。手机独立相机、箱子间距与点选代理，不整页缩放。报价底部全宽卡不遮关键箱子；底栏避开安全区、动态浏览器栏。横竖屏切换不重建对局，触控不依赖 hover。

## 页面与阶段交互

| 页面/阶段 | 展示与操作 | 等待和异常 |
|---|---|---|
| 启动/加载 | 必要资源进度、重试 | 原因、降级入口，不无限转圈 |
| 首页/模式 | 按真实可用性展示模式与规则 | 未实现模式明确不可用，不伪造房间 |
| 选个人箱 | 当前任务、焦点/待选编号、确认 | 服务端确认后才归属玩家 |
| 开箱 | 本轮剩余数、合法可开箱 | 禁止个人/锁定/已开箱；提交显示等待 |
| 报价 | 金额、来源、对白、截止时间 | 接受报价 / 拒绝并继续；处理中禁重复，过期说明 |
| 最终选择 | 区分个人箱与可交换箱 | 保留 / 交换；等待权威结算 |
| 结算 | 收益、原始与最终箱、成交价、历史最高报价、决策摘要 | 校验区分未校验/通过/失败/不可用；再来一局/返回 |
| 断线/重连 | 连接状态、适用托管规则和恢复剩余时间 | 快照恢复；失败提供重试/返回，不自动创建替代新局 |

报价时不展示矛盾的“本轮还需开箱”行动指令。金额列表仍可查看；已开金额用文字/划除辅助灰色。活动记录仅含公开事件，移动端收纳为入口，不以固定滚动条遮挡操作。
房间、角色交换、玩家报价、密封竞拍、观战、战绩、排名、设置、维护和分享已按 GDD §10 分阶段接入；当前仍以 ROADMAP 的“待验证”为准，未把业务代码等同于设备和安全验收。

## 组件完整状态

- 箱子：关闭、键盘聚焦、待确认、个人箱、对手箱、锁定、可开、打开中、已打开。打开中仅为表现状态。
- 按钮：可用、聚焦、按下、请求中、禁用；禁用说明原因。关键成交支持设置中的二次确认。
- 报价卡：有效、提交中、过期、成交、拒绝。倒计时依据服务端 deadline；客户端归零不裁决。
- 金额面板：未揭晓、已揭晓、加载/空/错误；不暗示选择金额能操控结果。
- 抽屉/弹窗：可访问名称、焦点进入和返回、键盘退出，提交中保留合法反馈。
- 网络/资源：连接中、断开、恢复中、失败、重试，解释发生了什么和下一步。

## 3D、动画与资产

2026-09-08 确认的箱子形制见 [概念图](art/chest-standard/concept/briefcase-approved-2026-09-08.png)：薄型手提箱关闭时竖立，正面朝舞台中心；原位放下底座，再打开上盖至约 100°。金额由 HTML 固定在活动上盖内侧，不能放在底座或展开为 180°。编号、金额均由运行时提供。

用户追加确认的精致方向：[演播厅](art/stage-basic/concept/studio-approved.png)、[珠宝箱细节](art/chest-standard/concept/jewelry-approved.png)。深紫细纹外壳、香槟金五金、绒面内衬；弧形主墙与中央标志、分层台阶和独立箱座、两侧柔和幕布；环境稍暗、箱面清晰，装饰保持克制。参考图中的编号和金额偏小，实际制作必须放大。金额牌约 1.19×0.49m，最长 `1,000,000` 使用完整数字并按长度缩放字号，不截断或省略。选择时局部提亮，桌面保留接触阴影，低档关闭实时阴影。

箱宽 1.40m，独立台座 1.78×1.46m；正式 26 箱按前后 6/7/7/6 排列。布局数据与 Blender 台座共用生成源，以完整开箱包围范围核验间距和中心相机投影。手机保持舞台总览，另提供至少 44px 的箱号选择与操作入口；小尺寸总览不承担唯一的金额阅读和点击入口。

制作使用[资产技能](.agents/skills/blender-web-game-asset-pipeline/SKILL.md)，登记 [ASSETS](docs/ASSETS.md)。新资产先概念图，可用设计不重复生成。

- 复用基础 GLB，以材质/编号参数区分，不制作 26 个独立编号模型。
- 静态批量箱与独立开盖动画需兼容：可用静态实例加当前独立动画箱，或测量通过的等效方案。
- 真实厚度、功能轴心、低成本点击代理、UI/FX 挂点齐全；关键文字不烘成不可访问贴图。
- 服务端公开结果后才揭晓；动画定时器不决定游戏阶段。重连直接恢复当前状态。
- 降低动态模式去除闪烁、摇晃和大幅镜头切换；声音可关闭，后台暂停非必要动画和音频。
- 交付 .blend、GLB、必要纹理、预览、动画清单与对应版本的验证报告。

## 性能与验收

桌面标准设备目标 60 FPS，中端手机 30–45 FPS，节能档可接受 30 FPS。高质量资源按需加载；降低阴影、粒子、像素比和后处理。HTML 操作不等待 3D 动画完成才响应。

记录设备、浏览器、画质、尺寸、同屏实例数和测量方法。检查完整一局、手机无溢出、安全区、横竖屏不丢状态、键盘/触控、最长金额/大字体、重复提交、报价过期、断线及 3D 失败反馈。展示与权威状态一致。静态阅读或截图不代表性能通过。
