# V0.4 / V1.0 代码回归记录

日期：2026-09-11。该记录只证明本地代码和服务集成检查，不替代真实设备、网络、性能、生产安全或发布验收。

## 已执行

- `npm run typecheck:server`：通过。
- `npm run test:server`：69 项通过，包含 `auction_26` 房间、密封报价、配置隔离、26 箱轮次、弃权、幂等、秘密隔离及原 Classic/Duel 回归。
- `npm run test:web`：67 项通过。
- `npm run test:pwa`：5 项通过，覆盖 API/游戏协议不缓存、离线应用壳、更新消息和分享白名单。
- `npm run test:database`：11 项真实 PostgreSQL 集成通过，覆盖赛季/Elo、竞拍与双人排名、举报、风险标记、管理员会话、审计、Outbox 和重启历史。
- `npm run build`：TypeScript、Vite 和 PWA 资源清单生成通过。
- `docker compose --env-file .env.production.example config`：Compose 服务、健康检查、卷和环境变量解析通过。
- 收尾补丁后的 `npm run typecheck:server`：通过；覆盖竞拍分享标识归一、排行榜 `seasonId` 查询和封禁房间准入/解封排除重算。

## 未证明

- 未执行真实桌面/手机/平板设备的帧率、帧时间、触控、旋转和安全区验收；当前桌面开发环境历史初测约 21 FPS，仍低于目标。
- 未完成真实公网 HTTPS/WSS、代理升级、断网/重连、资源失败和服务重启恢复验收。
- 未完成生产 Docker 镜像启动、数据库备份恢复、回滚、日志审计和管理员权限渗透检查；当前只做 Compose 配置解析。
- 未把 V0.4 或 V1.0 标记为已完成，相关进度仍为“待验证”。
